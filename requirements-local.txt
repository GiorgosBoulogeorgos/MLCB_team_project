# Local environment (e.g. Apple Silicon M3) for the local, off-Colab runs:
# Phase B Step 3 (per-donor LIANA / tensor) and Phase C rigorous CV.
# Needs Python >= 3.10.
#
#   conda create -n mlcb python=3.11 -y && conda activate mlcb
#   pip install -r requirements-local.txt
#
# Phase B Step 3:
#   python src/phaseB_step3_local.py --checkpoint-dir data/checkpoints --elbow
# Phase C rigorous:
#   python preflight_check.py --checkpoint-dir data/checkpoints
#   python diagnose_tensor_transform.py --checkpoint-dir data/checkpoints
#   python run_rigorous_local.py --sets comm
#
# On Apple Silicon, if xgboost complains about OpenMP: `brew install libomp`.
# Versions are left open; for exact Colab parity, `pip freeze | grep -Ei
# 'liana|cell2cell|tensorly|scanpy'` in Colab and pin those here.

# Phase B (LIANA / tensor construction)
liana
cell2cell
pyarrow
pandas
scipy

# Phase C (rigorous CV pipeline)
scikit-learn
optuna
xgboost
tensorly
mrmr_selection
shap
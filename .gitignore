# Data — lives on Drive, never in git
data/
*.h5ad
*.mtx
*.mtx.gz
*.csv.gz
*.soft.gz
*.tar

# Models
models/*.pkl

# Python noise
__pycache__/
*.pyc
.ipynb_checkpoints/
.env

# macOS noise
.DS_Store

# Docs (handoff notes live on Drive) — but keep the project report dir
MLCB*
!report/
!report/**

# LaTeX build artefacts
report/*.aux
report/*.log
report/*.out
report/*.toc
report/make_docx.py
